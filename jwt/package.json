{
  "name": "jwt",
  "version": "1.0.0",
  "description": "``` $pip install jwt $python gen_jwt.py ```",
  "main": "gen_jwt.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "jsonwebtoken": "^9.0.2"
  }
}
